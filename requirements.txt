# Root-level requirements for the Streamlit app (streamlit_app.py).
# Streamlit Community Cloud only auto-installs a file named exactly
# "requirements.txt" at the repo root.
streamlit>=1.36
onnxruntime>=1.17
tokenizers>=0.15
numpy>=1.26
huggingface_hub>=0.23
